---
id: scripts
title: Scripts
sidebar_label: Scripts
description: Scripting the Kortecx runtime.
---

# Scripts

Every Kortecx capability is reachable from the CLI and both SDKs with
machine-readable JSON output (`--json`), so the runtime scripts cleanly into
pipelines and automation.

:::note Coming soon
A scripting guide — composing `kx` invocations, JSON output contracts, and the
SDK ergonomics for batch and automation workflows — lands with a later docs PR.
For now, see the [Quickstart](./quickstart.md) and the
[CLI reference in the README](https://github.com/Kortecx/kortecx/blob/main/README.md#cli-reference).
:::
