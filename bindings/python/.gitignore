# Python build + cache artifacts (the generated *_pb2 stubs under
# src/kortecx/_proto/ ARE committed — they are the shipped contract — so they
# are deliberately NOT ignored here).
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.venv/
venv/
.env
.mypy_cache/
.ruff_cache/
.pytest_cache/
