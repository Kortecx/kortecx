/**
 * Legacy in-memory fallback store.
 * Kept for type reference — no longer pre-populated with mock data.
 */
import type { ProjectRecord } from '@/lib/api-client';

export const projectsStore: ProjectRecord[] = [];
