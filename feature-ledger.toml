# feature-ledger.toml — the OSS-lane feature registry (Golden Rule 25).
#
# One source of truth for what is in-flight / merged on the OSS shared surface +
# the cross-lane SEAMS, so concurrent sessions can SEE state and INHERIT/EXTEND
# prior work instead of re-building or colliding. Read this BEFORE starting a
# feature; update the entry's `state` as it progresses; declare `inherits` so the
# dependency graph is explicit.
#
# This file carries only OSS-public + seam entries (no cloud internals). The cloud
# lane has its own ledger in the cloud repo; the PRIVATE master ledger
# (docs/feature-ledger.md, corpus) reconciles BOTH lanes.
#
# Fields:
#   id        stable kebab-case identifier
#   lane      "oss" (public shared surface) | "seam" (cloud→OSS push-the-seam)
#   state     "planned" | "in-progress" | "in-review" | "merged"
#   pr        OSS PR number, once opened
#   branch    working branch
#   covers    capabilities this feature provides (what a later feature can reuse)
#   inherits  feature ids this builds on
schema = 1

[[feature]]
id       = "oss-cloud-sync-infra"
lane     = "oss"
state    = "merged"
pr       = "262"
branch   = "feat/oss-cloud-sync-infra"
covers   = [
  "shared-paths.toml boundary manifest (shared/private_only/divergent)",
  "scripts/shared-paths.sh reader",
  "just port / cmp-shared / leak-check / shared-lane-status",
  "crates/kx-cli/tests/shared_boundary.rs guard",
  "scripts/lane-guard.sh no-cross-merge gate",
  "feature-ledger.toml registry",
]
inherits = []

[[feature]]
id       = "serve-engine-public-seam"
lane     = "seam"
state    = "retired"
covers   = [
  "pub trait ServeEngine",
  "EngineRegistration + ServeEngineFactory",
  "extra_engine_factories hook in build_serve_runtime / GatewayConfig",
]
inherits = ["oss-cloud-sync-infra"]
notes    = "RETIRED — D183 (2026-07-05) CANCELLED this seam: even doc-hidden + default-off, a ServeEngineFactory / extra_engine_factories hook is an enable-able BYO-engine mechanism physically present in the PUBLIC repo (exactly what D179.6 forbade). OSS ships llama.cpp + ollama ONLY, no registration seam; the cloud implements the ALREADY-public kx_inference::InferenceBackend privately + builds its own serve/route/lifecycle stack (no OSS change). Do NOT build this. See decisions.md#d183 + the master docs/feature-ledger.md."

[[feature]]
id       = "connector-extension-sdk"
lane     = "seam"
state    = "merged"
pr       = "264"
branch   = "feat/seam-extension-sdk"
covers   = [
  "crates/kx-extension-sdk: FFI-free leaf crate (D167 E0)",
  "prelude: curated + api_surface-pinned re-export of the connector seams",
  "conformance: run_conformance + the 10-pt-gate subset (items 3/5/7/10)",
  "kx-connector-example reference connector ([[bin]]) — author template + positive control",
  "flow().with_mcp() / flow().withMcp() — connectors reachable from the chaining entry point",
  "kx.connections.{add,list,test,remove,discover} sub-client (Py + TS)",
  "just test-connector / test-connector-real / test-connector-live",
  "connector-conformance CI gate (pinned real external MCP, offline)",
  "gateway-admin feature: opt-in McpGatewayAdmin/ToolRegistryAdmin (host authoring)",
]
inherits = ["oss-cloud-sync-infra"]
notes    = "D167 E0 / D170 foundation seam #1. Curates the (already-pub, scattered) connector seams as a semver'd contract so parallel sidecars build against a stable surface. Digest 7d22d4bd invariant (leaf crate, re-exports + with_mcp add no journal facts); frozen trio untouched; no proto/journal change; additive-only."

[[feature]]
id       = "connector-live-toolcalling"
lane     = "oss"
state    = "merged"
pr       = "265"
branch   = "feat/connector-live-toolcalling"
covers   = [
  "T-CONNECTOR-AUTOGRANT-LIVE-DEADLETTER fix: ambiguity is a precise DecodeError::Ambiguous (candidate full-ids) → disambiguating A2 re-prompt, not a silent dead-letter",
  "kx-toolcall markerless-paren format slice (T-GEMMA-PAREN): NAME(kwargs) for dialed <server>/<remote> tools",
  "view-layer dead-letter reason synthesis (kx-gateway-core react.rs) — every dead-letter carries a reason",
  "CallMcpTool RPC: operator diagnostic live-fire of one dialed tool through the broker (SN-8 re-enforced; no journal fact)",
  "cross-surface: kx connections fire | kx.connections.fire (Py) | connections.fire (TS) | UI ConnectionsPanel live-fire",
]
inherits = ["connector-extension-sdk"]
notes    = "Makes a runtime-dialed external connector actually FIRE in the live react-auto loop (was a turn-0 dead-letter), plus a model-free CallMcpTool diagnostic. Digest 7d22d4bd invariant (react turns edge-free; advisory reason bytes off-digest); frozen trio untouched; additive-only proto + codegen-fresh."

[[feature]]
id       = "integrations-foundation"
lane     = "oss"
state    = "merged"
pr       = "266"
branch   = "feat/d170-integrations-foundation"
covers   = [
  "MM-3 local secret store: OS-keychain SecretStore (keyring native backends; keyutils headless) behind the kx-mcp SecretStore seam + ChainedSecretStore{keychain→env} back-compat",
  "PutSecret/ListSecretNames/DeleteSecret RPCs (value write-only; names+timestamps only on the wire; loopback-only writes) + secret_index.db NAME index",
  "D113 trigger seam: RegisterTrigger/ListTriggers/DeregisterTrigger/SubmitTrigger/TestTrigger + off-journal triggers.db (idempotency-key dedup, run-origin)",
  "HostTriggerAdmin: inbound event → fresh run via the Invoke propose-proxy (frozen trio untouched; binds under the registrant party D102.2)",
  "--webhook-listen: untrusted-inbound HTTP listener (HMAC-SHA256 X-Kx-Signature-256 / bearer auth, payload cap, per-trigger rate-limit, idempotency dedup; NONE only on loopback) + cron interval ticker",
  "cross-surface: kx secrets | kx triggers (CLI) · kx.secrets/kx.triggers (Py) · client.secrets/client.triggers (TS) · Integrations hub UI · docs",
  "fix: stale per-model recipe grant retired on a model/engine switch with a reused --catalog-dir (T-STALE-RECIPE-MODEL-ROUTE)",
]
inherits = ["connector-extension-sdk", "connector-live-toolcalling"]
notes    = "D170 foundation seams (after the connector SDK): the local secret store + the trigger seam + the Integrations hub, so app sidecars can authenticate real services and be event-driven. Digest 7d22d4bd invariant (all new state off-journal: OS keychain + triggers.db/secret_index.db sidecars); frozen trio untouched; additive-only proto + codegen-fresh; keyring/hmac/sha2 deps cargo-deny clean."

[[feature]]
id       = "autonomy-safety-gates"
lane     = "oss"
state    = "merged"
pr       = "267"
branch   = "feat/autonomy-safety-gates"
covers   = [
  "D114 HITL pre-action approval: off-DAG journal Approval fact (kind 10, Requested→Granted/Denied/Expired; journal v14→v15) + ReactRound.require_approval posture; deterministic recovery-stable request_id",
  "coordinator gate barrier: auto-proceed read-only/self-closing (Token/Readback), withhold irreversible world-mutating (AtLeastOnce/Staged) until operator grant (fires once via StageThenCommit) / deny dead-letters; grant survives crash-recovery, never re-asks",
  "M11 cost-spend guardrail: kx-pricing leaf crate (integer micro_usd price-book) + coordinator pre-stage cost-ceiling fold over the durable turn/tool counters (D115.2; a runaway kill-switch + FinOps ceiling, NOT Cloud billing)",
  "posture toggle: KX_SERVE_REQUIRE_APPROVAL serve default (per-run require_approval override) resolved off the seed-identity path",
  "proto: ListPendingApprovals/GrantApproval/DenyApproval + GetRunCost (additive, SN-8 server-derived request_id) + codegen-fresh (Py+TS)",
  "gateway: ApprovalAdmin seam + 4 handlers (unimplemented-degrade) + HostApprovalAdmin over the embedded coordinator",
  "cross-surface: kx approvals | kx cost (CLI) · kx.approvals/kx.cost (Py) · client.approvals/client.cost (TS)",
]
inherits = ["integrations-foundation"]
notes    = "The autonomy safety gates (D114 + M11): the last safety prerequisite before POC-GATE. Digest 7d22d4bd invariant (Approval facts + require_approval off-DAG; checkpoint v8→v9 zero-record byte-identical; cost is a pure off-journal fold); frozen trio src diff EMPTY (executor forwards the additive EffectRequest field); I1.c byte-determinism held; additive-only proto + codegen-fresh; full just ci green. DEFERRED follow-ups: UI (Security approvals inbox + Monitoring cost panel) · docs pages · flow()/agent() authoring-chain + per-run cost_ceiling request field · broker step-7 backstop · live-Gemma/MCP block→grant→fire witness."

[[feature]]
id       = "rc4c-2a-rerank-substrate"
lane     = "oss"
state    = "merged"
pr       = "275"
branch   = "feat/rc4c-2a-rerank-substrate"
covers   = [
  "ReRankRound durable journal fact (kind 11, journal v15→v16) + ReRankOutcome{Pending|Reranked{permutation}|FailedClosed}; hand-rolled encode/decode + v15→v16 pure-passthrough migration + pinning test (schema_version_is_v16)",
  "projection ReRankRoundRecord + off-DAG fold + rerank_rounds/rerank_rounds_of/latest_rerank_round accessors",
  "checkpoint v9→v10 (rerank_rounds DTO + From impls + roundtrip); digest 7d22d4bd invariant (a0_guard); frozen trio untouched",
  "ListReRankTurns RPC + ReRankTurnSummary (additive proto, fields 1-8) + Rust(tonic)/TS/Py codegen",
  "kx-gateway-core rerank.rs list_rerank_turns (instance filter + pagination, 6 tests) + tonic service arm; dep_wall green",
]
inherits = ["autonomy-safety-gates"]
notes    = "RC4c-2 SPLIT (RC4c-2a): the DURABLE rerank substrate + ListReRankTurns observability — the journal bump lands isolated (corpus-preferred). The LIVE coordinator rerank-turn is rc4c-2b. Digest 7d22d4bd invariant; frozen trio diff-EMPTY; additive-only proto + codegen-fresh; no external dep. Once merged, unblocks rc4c-2b on the shared lane and lets sidecars/cloud proceed in their own lanes without colliding on the journal schema (v16)."

[[feature]]
id       = "rc4c-2b-rerank-live"
lane     = "oss"
state    = "merged"
pr       = "278"
branch   = "feat/rc4c-2b-rerank-live"
covers   = [
  "coordinator-materialized off-DAG rerank Mote (react_shape::build_rerank_turn + kx-rerank-turn id salt) + anchor/materialize/settle/recover (ReRankRound Pending→Reranked/FailedClosed)",
  "model_exec rerank dispatch arm (render_rerank_prompt + Grammar::Permutation(n) + parse_permutation → committed reordered refs)",
  "react-rag insertion (post-retrieve obs, off-budget) + chat-rag pre-answer insertion + F-7 context-feeding of reranked refs",
  "KX_SERVE_RAG_LLM_RERANK operator env + coordinator enable-flag path",
  "kx-eval RAG-quality rerank scorer (deliberate suite_digest re-baseline) + kx-profile M7c spike + gated dual-engine Gemma e2e",
  "cross-surface: kx rerank list (CLI) · list_rerank_turns (Py+TS) · Monitoring ReRankRounds UI · docs llm-rerank.md",
]
inherits = ["rc4c-2a-rerank-substrate"]
notes    = "RC4c-2 SPLIT (RC4c-2b): the LIVE coordinator rerank-turn (both react-rag + chat-rag), building on rc4c-2a's ReRankRound fact + ListReRankTurns. A sole-writer-spine change ⇒ its own focused, recovery-tested, dual-engine-validated PR. Fully MAPPED (materialize/settle/recover mirror of the ReAct lifecycle) — a direct build, not a re-exploration."

[[feature]]
id       = "rc4c-2c-rerank-quality"
lane     = "oss"
state    = "merged"
pr       = "279"
branch   = "feat/rc4c-2c-rerank-quality"
covers   = [
  "rerank quality + dual-engine parity (llama.cpp Gemma-4 renders the rerank prompt through the served chat template) + fail-closed regression guard (kx-eval rerank_quality scorer + golden rag_rerank_on_topic_last)",
  "cap clamp to warrant ceiling + trailing-tolerant parse_permutation + kx-worker dispatches_through_executor guard + rerank_serve both-engines-reranked + kx-profile M7c",
]
inherits = ["rc4c-2b-rerank-live"]
notes    = "Digest 7d22d4bd invariant; frozen trio untouched; NO journal/checkpoint/proto/dep change; deliberate eval re-baseline (loop_efficiency 964→968). Consolidates RC4c-2b to SOTA."

[[feature]]
id       = "rc5a-durable-memory"
lane     = "oss"
state    = "merged"
pr       = "280"
branch   = "feat/rc5a-durable-memory"
covers   = [
  "kx-memory crate: SqliteMemoryStore (memory.db rebuildable sidecar; rebuild-never-migrate D40)",
  "StoreMemory/ListMemories/RecallMemory/ForgetMemory RPCs + MemoryView gateway-core seam (no kx-memory dep on core)",
  "recall@1 / remember@1 bundled capabilities + kx/recipes/react-memory (0x5c); KX_SERVE_MEMORY env",
  "cross-surface: kx memory (CLI) · kx.memory (Py sync+async) · client.memory (TS) · Memories UI · flow().with_memory / withMemory · docs/site/docs/memory.md + kx-eval memory_quality scorer",
]
inherits = ["rc4c-2c-rerank-quality"]
notes    = "Durable multi-tier memory (semantic recall + episodic store). Digest 7d22d4bd invariant (memory.db off-journal/off-digest, NO journal schema bump — the retrieve@1 precedent); frozen trio untouched; additive-only proto + codegen-fresh."

[[feature]]
id       = "rc5b-consolidation-decay-memory-ui"
lane     = "oss"
state    = "merged"
pr       = "281"
branch   = "feat/rc5b-consolidation-decay-memory-ui"
covers   = [
  "kx-memory: bundle (recency + semantic, kind/window-filtered, SN-8 score-dropped) + reversible TTL+salience decay (memory_decay off-digest sidecar table; soft-tombstone + restore; decay_all) + stats + list(include_tombstoned) + best-effort recall salience upsert",
  "consolidate@1 capability (Readback; model bundles episodics → distills → remember@1; no journal fact) granted to react-memory (3 tools); server seed + register + opt-in decay auto-sweep-on-open (KX_MEMORY_DECAY_AUTO/TTL_DAYS/MIN_ACCESS)",
  "additive proto: DecayMemory/MemoryStats/RestoreMemory + MemorySummary salience/tombstone fields + ListMemories.include_tombstoned; MemoryView seam bundle/decay/stats/restore; unimplemented-honest degrade",
  "cross-surface: kx memory decay/stats/restore/consolidate + list --include-tombstoned (CLI) · Py sync+async · TS · Memories-tab StatsStrip/DecayPanel(CHIP)/ConsolidatePanel + restore + vitest · docs/site/docs/memory.md decay+consolidation",
  "kx-eval consolidation_quality scorer + golden memory_consolidate_episodics (deliberate re-baseline loop_efficiency 972→975) + kx-memory salience-write hot-path probe (GR10) + dual-engine consolidate_serve e2e",
]
inherits = ["rc5a-durable-memory"]
notes    = "RC5b consolidation + decay + memory-UI. Model-driven consolidation (no journal fact) + reversible decay (memories rows never deleted; journal re-fold restores). Digest 7d22d4bd invariant (all new state in memory.db + memory_decay sidecar, off-journal/off-digest); frozen trio EMPTY; NO journal/checkpoint bump; additive-only proto + codegen-fresh."

[[feature]]
id       = "gmail-integration"
lane     = "oss"
state    = "merged"
pr       = "276"
branch   = "feat/gmail-connector-sidecar"
covers   = [
  "crates/kx-connector-gmail: a bundled Gmail MCP connector — a standalone [[bin]] stdio JSON-RPC 2.0 server (initialize -> tools/list -> tools/call) exposing gmail/{search,read,draft,send}; FFI-free (serde_json/ureq/base64/thiserror only), NO kx-* runtime dependency (not in the gateway or frozen-trio closure)",
  "credential-by-reference (D81): OAuth {client_id,client_secret,refresh_token} injected as KX_GMAIL_CREDENTIAL, refresh->access exchanged INSIDE the sidecar; secret never in a reply/log/error; header-injection guard on to/subject",
  "offline KX_GMAIL_FAKE mode (canned responses) → deterministic unit tests + the run_conformance gate (Extension Acceptance Gate items 3/5/7/10) with a live-#[ignore] dual-engine witness deferred to registration",
]
inherits = ["connector-extension-sdk", "integrations-foundation"]
notes    = "PR-G0 (extension lane, isolated per the user's core/extension split): the FIRST bundled app-connector sidecar. Digest 7d22d4bd invariant (a0_guard PASS — the crate is an external process, outside the projection workspace); frozen-trio untouched; no proto/journal change; Cargo.lock += only the crate node (no new external crate; serde_json `raw_value` feature adds no dep); cargo-deny clean. Runtime behaviour = a dialed external MCP process (bucket i). NEXT (separate sessions, core changes): G1 first-class Gmail Integration UI/CLI/SDK, G2 App-pointer->run wiring (references.connections + guards.secret_scope), G3 cross-instance import."

[[feature]]
id       = "discord-connector"
lane     = "oss"
state    = "merged"
pr       = "277"
branch   = "feat/connector-discord"
covers   = [
  "integrations/kx-connector-discord: a bundled Discord MCP connector — a standalone [[bin]] stdio JSON-RPC 2.0 server (initialize -> tools/list -> tools/call) exposing discord/{send_message,read_channel,list_channels}; FFI-free (serde/serde_json/ureq/thiserror only, no base64), NO kx-* runtime dependency (not in the gateway or frozen-trio closure)",
  "credential-by-reference (D81): a bot token injected as KX_DISCORD_CREDENTIAL ({bot_token}), sent as `Authorization: Bot <token>` INSIDE the sidecar; secret never in a reply/log/error; snowflake-id guard (digits-only channel_id/guild_id) blocks URL path-injection + a 2000-char content bound",
  "offline KX_DISCORD_FAKE mode (canned responses) → deterministic unit tests + the run_conformance gate (Extension Acceptance Gate items 3/5/7/10) with a live-#[ignore] witness deferred to registration",
]
inherits = ["connector-extension-sdk", "integrations-foundation", "gmail-integration"]
notes    = "The SECOND bundled app-connector sidecar (the template the Slack/Notion connectors clone). Extension lane, isolated per the core/extension split. Digest 7d22d4bd invariant (a0_guard PASS — external process outside the projection workspace); frozen-trio untouched; no proto/journal change; Cargo.lock += only the crate node (all deps already in the tree — no new external crate); cargo-deny clean. Runtime behaviour = a dialed external MCP process. Usable TODAY via `kx connections add --command kx-connector-discord --credential-ref KX_DISCORD_CREDENTIAL`; first-class/by-pointer UX lands with G1/G2/G3 below."

[[feature]]
id       = "slack-connector"
lane     = "oss"
state    = "merged"
pr       = "291"
branch   = "lane/integrations"
covers   = [
  "integrations/kx-connector-slack: a bundled Slack MCP connector modelled on kx-connector-discord — slack/{post_message,read_channel,search,list_channels}; bot-token (xoxb-) credential-by-ref KX_SLACK_CREDENTIAL; FFI-free leaf, no kx-* dep",
]
inherits = ["discord-connector"]
notes    = "Extension lane, same template as discord-connector. Zero core blast radius (digest 7d22d4bd invariant, frozen-trio untouched, no proto/journal change)."

[[feature]]
id       = "notion-connector"
lane     = "oss"
state    = "merged"
pr       = "291"
branch   = "lane/integrations"
covers   = [
  "integrations/kx-connector-notion: a bundled Notion MCP connector modelled on kx-connector-discord — notion/{search,read_page,create_page,append_block}; integration-token credential-by-ref KX_NOTION_CREDENTIAL; FFI-free leaf, no kx-* dep",
]
inherits = ["discord-connector"]
notes    = "Extension lane, same template as discord-connector. Zero core blast radius (digest 7d22d4bd invariant, frozen-trio untouched, no proto/journal change)."

[[feature]]
id       = "first-class-integrations"
lane     = "oss"
state    = "merged"
pr       = "282"
branch   = "feat/g2-g1-app-pointer-run-and-first-class-gmail"
covers   = [
  "G1 (Gmail slice) — a curated 'Connect Gmail' provider shortcut: `kx connections add --provider gmail` (static provider catalog id -> command + credential-ref) + a ConnectionsPanel 'Connect Gmail' prefill chip + the App-builder `.with_gmail()` (Py+TS), all over the EXISTING RegisterMcpServer + PutSecret RPCs (no new proto)",
  "the static provider catalog (provider id -> stdio command + credential-ref name) mirrored across CLI/SDK/UI; Discord/Slack/Notion clone the Gmail row when curated",
]
inherits = ["discord-connector", "integrations-foundation"]
notes    = "LOW blast radius: client-side curation over existing RPCs. Reuses ui/src/components/tools/ConnectionsPanel.tsx + crates/kx-cli/src/verbs/connections.rs + the SDK App builders. Off-journal; digest 7d22d4bd invariant. Bundled WITH G2 (app-pointer-run-resolution) in one PR (the connect -> build App -> run vertical). Merged OSS #282 fb3dc484 (2026-07-02)."

[[feature]]
id       = "app-pointer-run-resolution"
lane     = "oss"
state    = "merged"
pr       = "282"
branch   = "feat/g2-g1-app-pointer-run-and-first-class-gmail"
covers   = [
  "G2 — carry the App envelope's references.connections + guards.secret_scope through the RUN path so a running App resolves its connection-pointers to the caller's OWN registered connection (by credential-ref name) and sets the run warrant's SecretScope::AllowList to the App's secret_scope names (so a credentialed connector can be dialed inside the agentic loop)",
  "an additive server-side RunApp(handle,args) RPC + a host AppAuthor seam (HostAppAuthor) — NOT RecipeBinder::bind: verified in code that Apps run via SubmitWorkflow -> WorkflowAuthor::author, so the server owns the validated envelope (SN-8, no client-forged refs) and lowers its blueprint through the SHARED kx-blueprint path; the tool-firing warrant's secret_scope is set post-author (a fresh grant, no WorkflowAuthor signature change)",
  "a new FFI-free kx-blueprint crate (DagSpec + lowering extracted from kx-cli so the server can lower a stored blueprint identically); connection-by-name resolution over the connections store (own read handle, WAL) with missing-integration + loud-mis-authoring guards; CLI/Py/TS run_app -> RunApp with a legacy GetApp->SubmitWorkflow fallback on UNIMPLEMENTED; UI missing-integration action",
]
inherits = ["first-class-integrations", "gmail-integration"]
notes    = "MEDIUM blast radius: additive RunApp RPC + a host seam + the shared author_steps_from_proto extraction (submit_workflow refactor is behaviour-preserving). Off-journal + digest 7d22d4bd invariant (connections/secrets off-DAG; secret_scope IS content-addressed into the warrant_ref but the new path defaults to None ⇒ existing runs byte-identical + the kill-and-replay fixture untouched; recovery replays the journaled warrant_ref). Frozen-trio EMPTY. Cargo.lock += only the kx-blueprint node. Bundled with G1 (first-class-integrations). Merged OSS #282 fb3dc484 (2026-07-02)."

[[feature]]
id       = "cross-instance-app-import"
lane     = "oss"
state    = "planned"
covers   = [
  "G3 — `kx app import <file>` + an additive import RPC (proto currently states 'NO cross-instance import') that re-binds a shared kortecx.app/v1 envelope against the IMPORTER's own by-name connections/secrets, with a 'missing integration' UX listing what the importer must configure",
  "import validation: authority-leak already prevented by AppEnvelope::validate + secret_leak.rs; credential-namespace collision (importer's secret wins); warrant/principal spoofing (importer's warrant must cover the App's requested grants, server-side, before bind)",
]
inherits = ["app-pointer-run-resolution"]
notes    = "MEDIUM blast radius + a real security surface (security review required). The 'share an App to others; the runtime uses THEIR own integration details by pointer' story. Off-journal + additive (no journal fact); digest 7d22d4bd invariant. Per-INSTANCE model (each person runs their own kx serve) is the OSS path; multiple users on ONE instance with isolated per-person credentials = Cloud (D129)."

[[feature]]
id       = "rc-sw1-skills"
lane     = "oss"
state    = "merged"
pr       = "284"
branch   = "feat/rc-sw1-skills"
covers   = [
  "kortecx.skill/v1 declarative skill bundles (instructions + tool grant-WISHES + context; NO code — the executable leg is always an out-of-process MCP connector/broker capability, D159/D174.4): a NEW FFI-free kx-skill format crate (manifest type + validate() with authority deny-keys + pack loader; the apps.db/kx-blueprint leaf posture)",
  "the shipped-but-inert SkillRef (kx-app envelope references.skills) binds in HostAppAuthor::author_app — instructions_ref -> entry-step context + a skill_union_warrant from wish INTERSECT caller-Use-grants INTERSECT fireable_grants() applied POST-author (the exact G2 secret_scope pattern; the intersection MUST include the caller's own Use grants — SN-8)",
  "skills.db per-principal rebuildable catalog (apps.db posture) + a SkillCatalog gateway-core seam + additive ListSkills/GetSkillForm/AddSkill/RemoveSkill RPCs; cross-surface kx skills CLI + Py/TS + UI attach + docs/site/docs/skills.md",
  "run_skill_conformance harness (kx-extension-sdk sibling of run_conformance: declarative/no-authority/wish-resolvable/binds-empty-grants-to-zero) + just test-skill + 2-3 reference skill packs (skills/**) + a skill_quality kx-eval scorer + registry/index.json v1 (family/name/version/source/conformance)",
]
inherits = ["rc5b-consolidation-decay-memory-ui", "app-pointer-run-resolution", "gmail-integration", "connector-extension-sdk"]
notes    = "The NEXT shared-lane PR (D174.3 sequencing; D175 program). B-spec: private docs/plans/2026-07-02-capability-families-skills-tools-integrations-program.md SECTION 4 (start there — D143). CORRECTED naming per D175.2: dedicated kx-skill crate + skills.db, NOT kx-bundle/bundles.db (those are TaskBundle + the PR-7 context-bundle store). Auto-mode (skill menu + KX_SERVE_AUTOSKILL) DEFERRED to its own PR or RC-SW4. Honest digest note: skill-BEARING runs legitimately produce different bytes (warrant_ref content-addressed + identity-bearing context); canonical 7d22d4bd invariance holds via the default-empty no-op (a0_guard + frozen-trio-EMPTY prove in-PR). Unblocks RC-SW2 personas. Merged OSS #284 42ff3853 (2026-07-02); ledger flipped in-progress->merged during rc-sw2."

[[feature]]
id       = "rc-sw2-swarm-personas"
lane     = "oss"
state    = "merged"
pr       = "286"
branch   = "feat/rc-sw2-swarm-authoring-personas"
covers   = [
  "swarm()/team() authoring — first-class Py/TS SDK factories (kx.swarm/kx.team) that lower PURE CLIENT-SIDE to N parallel deterministic-agentic MODEL leaves (each a model step with a tool_contract + prompt = the is_agentic_launch shape the coordinator already drives as an isolated salt-2 chain, state.rs) fanned into one gather sink (PURE fold by default; a MODEL synthesizer when synthesize=True / team()). No new WorkflowStepKind, no coordinator/compile/proto change — the same (steps, edges) the string DSL '[a@t & b@t] > g' already lowers (agentic_multi_tool + fanin_group corpus rows); byte-identical across Py/TS/CLI via tests/golden/chains/corpus.json",
  "persona library — a curated {name: instructions} map (personas.py/personas.ts: researcher/critic/planner/writer/...) folded into an agent's PROMPT_KEY (identity-bearing, opt-in, new-motes-only => digest-invariant; presentation-only rejected as a dedup/replay hazard); consumed via kx.persona(name)->Agent, Agent(persona=name), and the .on(task) alias of Agent.as_flow/run",
  "fan_out_gather()/map_reduce() surfaced as client-side Flow compositions (the same fan-in family as swarm, plain generator/transform leaves)",
  "App.run() -> SaveApp + RunApp delegation fix (Py app.py + TS app.ts both locally recompiled via submit_workflow, DROPPING references.connections + guards.secret_scope and ignoring args) + an explicit Flow.as_app(name) persist boundary (no hidden catalog write; a bare .run() on an integration-bearing unsaved envelope is a clear error) => credentialed connectors now actually fire from the SDK path",
  "integration reach for parallel-session testing: App.with_discord() curated shortcut (kx-connector-discord exists, #277) + .secrets() sugar; a GENERIC connector RunApp live-harness (parametrize app_live_serve.rs runapp_gmail_... into runapp_connection_live(cmd,cred,probe) so any sidecar is testable); a `kx new connector` scaffold (verbs/new.rs, mirrors kx-connector-gmail; only `kx new skill` existed)",
  "Py<->TS parity fixes: Python Agent dynamic-lane max_tool_calls 6->20 (match TS + server default REACT_DEFAULT_MAX_TOOL_CALLS=20); TS module-level chat(); TS barrel export of REACT_RAG/VISION_RAG/react-vision recipe handles; stale chains.py docstring fix",
]
inherits = ["rc-sw1-skills", "app-pointer-run-resolution", "gmail-integration", "discord-connector", "first-class-integrations"]
notes    = "RC-SW2 (D174.3/D176/D177). PURELY ADDITIVE client-side composition + ONE runtime-behavior fix (App.run delegation). Digest 7d22d4bd INVARIANT BY CONSTRUCTION (swarm/personas touch only NEW motes; connections/skills/secrets ride OFF the lowered graph) + frozen-trio EMPTY + no proto/journal/WorkflowStepKind change; proven in-PR by a0_guard (8/8) + tri-surface golden parity. DEFERRED (own follow-ups, recorded so not re-litigated): .retry_until() critic-gated best-of-N -> RC-SW4 (needs a server kx/recipes/retry-until binder + free-param slots; critic kind is not in the client palette; natural home = supervisor/consensus); branch() -> RC-SW4/own PR (dynamic topology-shaper + EdgeKind::Control cascade, NOT pure composition — RC-SW3 was RE-SCOPED to parallel-local-exec per the freshest D178); effect=/idempotency= override hatch -> own D-number (effect_pattern is a MoteDef identity + executor double-effect/refusal gate; a client override is an SN-8 + un-deduped-double-effect risk); full Flow-level .connect()/.skill()/.secrets()/.use()/.context() sugar -> own D177 ergonomics PR; supervisor()/consensus()/handoff() -> RC-SW4; .with_slack() -> when kx-connector-slack lands (GR25 extension lane); T-SWARM-CHILD-SECRET-SCOPE-NARROW (narrow.rs, dynamic child secret_scope) -> RC-SW4. Merged OSS #286 8cc868c + the persona/Agent-leaf lowering fix #287 e6f3e06; ledger flipped in-progress->merged during t-runapp-context-rail (2026-07-03)."

[[feature]]
id       = "rc-sw3-parallel-local-exec"
lane     = "oss"
state    = "merged"
pr       = "288"
branch   = "feat/rc-sw3-parallel-local-exec"
covers   = [
  "bounded embedded-worker POOL in kx-gateway (server.rs): --workers N / KX_WORKERS / KX_SERVE_WORKER_POOL (env_caps::resolve_worker_pool, default 1 = byte-identical single worker), loop-spawns N (register, spawn_worker_loop) pairs into JoinSet vecs sharing ONE model executor Arc; each registers independently (distinct WorkerId + D56 LoadAwarePlacement). Turns authored parallelism (RC-SW2 swarm/parallel/fan_out_gather) into real wall-clock parallelism for Pure/IO/tool Motes; Ollama swarms get real concurrent inference over independent pooled HTTP",
  "the load-bearing coordinator change (state.rs lease_ready): a NEW LeaseTracker::is_leased_by_other admission filter partitions the ready set across pool workers (a Mote held by a LIVE OTHER worker is skipped) so two workers never redundantly run the same one; a worker may still re-lease its OWN holds (mid-batch-error self-heal) and reap re-offers a crashed worker's holds after the liveness window => pool=1 byte-identical (a lone worker is never an 'other' holder), coordinator stays SOLE journal writer, in-memory bookkeeping only (no journal/checkpoint/proto/digest change)",
  "RC-SW3 wall-clock per-Mote effect timeout (kx-worker): opt-in KX_SERVE_TOOL_DEADLINE_SECS (default OFF => byte-identical) wraps the async run_wm (tool/MCP/IO) arm in tokio::time::timeout; a hung dispatch is cancelled (exactly-once via broker idempotency-key dedup + R-13) and surfaced as the TRANSIENT WorkerError::ExecutionTimedOut (retried within the F4 budget, then dead-lettered) so a stuck external tool never pins a pool slot. Per-agent tool-call budget is ALREADY per-chain (each swarm agent = a salt-2 chain under the coordinator fold-re-derived REACT_MAX_TOOL_CALLS cap, recovery = restart-from-journal)",
  "llama.cpp concurrent inference SPIKE RESOLVED (2026-07-03, MEASURED on Apple M3 / Gemma-4-12B Q4_K_M): shared-weights concurrent Metal decode (one Model + 2 KV Contexts, 2 scoped threads, greedy) gave NO throughput gain — serial 29.7s vs concurrent 28.7–35.7s ≈ 0.83–1.03x (Metal serializes the GPU command queue). So llama.cpp stays SINGLE-LANE; NO --model-lanes pool built (kept as T-METAL-MULTI-LOAD, a cloud/CPU concern). Concurrent LOCAL inference is served by Ollama (independent pooled HTTP + OLLAMA_NUM_PARALLEL) via the worker pool — the pool still overlaps llama.cpp Pure/IO/tool turns",
  "cross-surface: ServerInfoFacts.worker_pool (static) surfaced in the console Settings panel (effectiveWorkerPool) — the EXISTING Monitoring 'In flight' metric already shows >1 concurrent in-flight motes during a swarm, so parallelism is visible live; a DEDICATED serve-wide live worker-count read field is deferred to RC6a (Monitoring/trace-tree) — + CLI kx serve --workers / kx status render (static pool size + --json) + Py/TS SDK observability ServerInfo.worker_pool/effective_worker_pool (NO Flow.pool — pool is serve-time infra, not topology) + docs/site/docs/parallel-execution.md",
  "bundled safety fix: RunApp->SubmitWorkflow no-args UNIMPLEMENTED fallback FAILS LOUD when the stored envelope declares references.connections/guards.secret_scope (py client.py + ts client.ts + cli app.rs) instead of silently running a de-integrated workflow",
  "MANDATORY gates: pool_determinism {lease-exclusivity + digest-invariance(pool in {1,2,4}) + crash-mid-swarm replay-determinism + worker-death-mid-swarm digest-invariance} + WM-timeout-through-R-13 + kx-profile pool-latency regression guard (pool1 == no-pool baseline); LIVE GR24 witness app_live_serve::swarm_pool_parallel_speedup_live (both engines, model restarted); keep a0_guard/audit_trail/checkpoint_recovery/runtime_parity/concurrency/recovery/reschedule/dead_letter/verify-quickstart/scale-smoke + swarm_agentic_gather golden green",
]
inherits = ["rc-sw2-swarm-personas", "rc-sw1-skills"]
notes    = "RC-SW3 (D178 completeness plan #1 CRITICAL / D176 sole exec-path touch). The freshest D178 (§2.302) RE-SCOPED RC-SW3 to parallel-local-exec (NOT branch(), which moves to RC-SW4/own PR). Digest 7d22d4bd INVARIANT: digest_projection sorts committed facts by MoteId (digest.rs:32) so execution/worker order is not a digest input; the coordinator stays the single owner thread + sole writer; default pool=1 + lanes=1 + timeout-off are byte-identical. NO journal (v16) / checkpoint (v10) / WorkflowStepKind / canonical-proto change (a static ServerInfoFacts field is an additive READ-only wire field, off-digest). GR8: mitigated double-lease (is_leased_by_other), hung-slot (wall-clock timeout + reap), multi-lane memory (separate --model-lanes knob default 1 + spike-gate). BLOCKER #4 respected: no in-memory count replaces the fold-re-derived per-chain budget. T-SWARM-CHILD-SECRET-SCOPE-NARROW stays RC-SW4 (own security PR). Merged OSS #288 62d30c79 (23/23 CI) / priv mirror mirror/oss-288-rc-sw3; ledger flipped in-progress->merged during t-runapp-context-rail (2026-07-03)."

[[feature]]
id       = "rc-sw3.1-resource-aware-backpressure"
lane     = "oss"
state    = "planned"
branch   = ""
covers   = [
  "resource-aware ADMISSION: NdClass-aware CPU/IO/model permit budgets + proactive permit-before-execute (leave a Mote queued in the ready set vs the reactive NoCapacity->backoff->retry churn) + fairness across chains/agents",
  "capability AUTO-TUNING (host-aware resource manager, user-requested 2026-07-03): startup HostProfile probe (cores/RAM/Metal recommendedMaxWorkingSetSize/GPU class/model sizes) -> derive worker_pool + model_residency + inference_concurrency + io_concurrency budgets where unset -> capability-gate with honest-degrade (concurrent-decode advertised only on GPU headroom; multi-model swarm only when RAM fits >=2 models) -> config overrides (--resource-profile {auto|laptop|workstation|server} + per-knob env). ONE codebase spans 16GB laptop -> Mac Studio -> cloud",
  "measured motivation (RC-SW3 session): 12B saturates one GPU (concurrent decode ~1.0x both engines; 4-agent decode swarm 0.64x); OLLAMA_NUM_PARALLEL=4 on 12B -> CPU-offload thrash ~40x. Auto-tuner MUST cap inference concurrency by gpu_headroom/per_slot_KV",
  "invariants: additive/execution-side/OFF-digest (admission + defaults only; coordinator stays sole writer; default-auto on 16GB ~= today; digest 7d22d4bd invariant). Reuses LocalResourceManager/ResourceCeiling/D56 LoadAwarePlacement/NdClass seams. Own GR8 + kx-profile(throughput vs pool/lanes/residency) + GR24 dual-engine",
]
inherits = ["rc-sw3-parallel-local-exec"]
notes    = "Committed follow-up to RC-SW3 (own PR; may split RC-SW3.1a admission / RC-SW3.1b auto-tune). Full design brief lives in the private SN-5 plan docs/plans/2026-07-03-rc-sw3-parallel-local-exec.md (RC-SW3.1 section). Sequenced after RC-SW3 close, before/beside T-RUNAPP-CONTEXT-RAIL per D178."

[[feature]]
id       = "t-runapp-context-rail"
lane     = "oss"
state    = "merged"
pr       = "289"
branch   = "feat/t-runapp-context-rail"
covers   = [
  "wire the App envelope's INERT knowledge rail into RunApp (D178 pillar #2 CRITICAL): HostAppAuthor::author_app (kx-gateway/src/app_run.rs) resolves references.context/prompts/rules/memory + steering_config.context.{context_refs,bundle_handles} into labeled ContextItemRefs merged through the SAME author_with_context_items extra_items seam RC-SW1 uses for skills (context:/prompt:/rule:/memory:/ref: prefixes; fail-closed CAS-presence per item) => an App self-grounds on its declaration instead of a hand-authored blueprint",
  "declarative RAG-on-App (reference-existing / 2a): references.datasets folds a retrieve@1 grant onto the entry agentic step (mirror fold_skill_tools) + a fail-closed dataset-presence check against the live DatasetView (Arc<dyn DatasetView> threaded into HostAppAuthor) + a prompt-steer naming the declared dataset(s) (mirror fold_react_rag_dataset). Non-hnsw build => honest degrade. Self-contained cas_refs ingest (2b) deferred to T-RUNAPP-RAG-SELF-CONTAINED",
  "steering_config.tools.requested_grants folded through the SAME skill_union_grants intersection (wish INTERSECT caller-Use INTERSECT fireable INTERSECT registry INTERSECT compat) onto the entry step; author-declared pins win; SN-8 (wish never authority)",
  "SDK .dataset()/.rag() (Py app.py slot exists :71; TS app.ts _datasets field + method + referencesDict branch, byte-identical) + a 'grounded' tests/golden/apps/corpus.json row (Rust/Py/TS) + builder unit tests",
  "UI full rail authoring in NewAppForm (a 'Ground on dataset' chip over ListDatasets, model on DatasetPicker.tsx, + context/rule attach) + read-only resolved-rail render in AppsSection",
  "per-App memory sub-scope: SPLIT to T-RUNAPP-APP-MEMORY-SCOPE (plan-mode review verdict) — Capability::invoke never sees the warrant (local.rs:275), so per-App memory needs new axes in TWO foundational crates (kx-warrant WarrantSpec + kx-capability broker-dispatch, ~124 sites, security-sensitive); too much blast radius to bundle. Recorded as a planned follow-up with the full mapped design",
  "verification: deterministic gates (context-merge digest-invariant-when-empty, retrieve@1 fold, steering.tools intersection, memory-scope byte-identical default) + a0_guard 7d22d4bd + frozen-trio EMPTY + pool_determinism 4/4 green + tri-surface golden + LIVE dual-engine Gemma witness (credentialed App self-grounds on context/dataset AND fires a real Gmail/Discord connector, model restarted per engine) + kx-eval grounded_app_quality scorer + kx-profile author_app-with-rail spike",
]
inherits = ["rc-sw1-skills", "app-pointer-run-resolution", "rc4c-2b-rerank-live", "gmail-integration", "discord-connector"]
notes    = "D178 completeness plan #2 (CRITICAL, pulled EARLY per the freshest D178 over the corpus's literal RC-SW3.1-next). The direct follow-on to RC-SW1: RC-SW1 wired references.skills through author_with_context_items; this wires the REMAINING dropped rails (.context/.prompts/.rules/.memory/.datasets + steering_config.context/.tools). Digest 7d22d4bd INVARIANT by construction (empty-rail => no inject => byte-identical; a rail-bearing App legitimately produces different Mote/warrant bytes = user data, the RC-SW1 precedent); frozen-trio UNTOUCHED; additive-only proto (none expected) + codegen-fresh if any. RC-SW3.1 resource-manager slots after (execution-side/off-digest, path-mostly-disjoint). Deferred: T-RUNAPP-RAG-SELF-CONTAINED (2b cas_refs ingest) + T-RUNAPP-APP-MEMORY-SCOPE (memory-scope split fallback)."

[[feature]]
id       = "t-runapp-app-memory-scope"
lane     = "oss"
state    = "planned"
branch   = ""
covers   = [
  "per-App durable-memory sub-scope: bind memory to the App handle (mem::<principal>::<app-handle> via memory_namespace(party, handle), gateway-core/src/memory.rs:60) vs the shared sub='' bucket (server.rs:1107); non-App runs byte-identical (empty sub => mem::<principal>, today's bucket)",
  "the seam (plan-mode-mapped): a per-run namespace is a DATA-routing input the recall/remember/consolidate capability must READ, but Capability::invoke sees ONLY EffectRequest, never the warrant (kx-capability/src/local.rs:275, capability.rs:61). So: (1) add memory_scope: Option<String> axis to WarrantSpec (kx-warrant/src/spec.rs:140, Default->None, pass-through in intersect/narrow, proptest + ~124 construction sites); (2) add memory_scope to EffectRequest (kx-capability/src/request.rs:22, #[serde(default)] — digest-safe, EffectRequest is a transient DTO, never journaled/content-addressed); (3) LocalCapabilityBroker::dispatch copies warrant.memory_scope into the owned request before cap.invoke (local.rs:270-278) + the two runtime builders add memory_scope: None (engine.rs:960, run_wm.rs:147); (4) recall/remember/consolidate read request.memory_scope.unwrap_or(&self.namespace); (5) author_app sets warrant.memory_scope = Some(memory_namespace(party, handle)) on tool-granting warrants (app_run.rs:466-472)",
  "digest/recovery: memory.db off-journal+off-digest (kx-memory/src/lib.rs:24); product digest 7d22d4bd folds ONLY mote_id|result_ref|nd_class, NOT warrant_ref (digest.rs:31-56, entry.rs:102-103) => invariant; BUT non-App warrant_ref BYTES change (bincode None discriminant) — needs its own warrant-ref/encode_warrant fixture-review + a memory-scope guard, the reason to SPLIT",
]
inherits = ["t-runapp-context-rail", "rc5b-consolidation-decay-memory-ui"]
notes    = "SPLIT out of t-runapp-context-rail by the subtask-8 plan-mode review: it touches TWO foundational crates (kx-warrant identity model + kx-capability broker-dispatch seam) that EVERY run flows through, whereas the context-rail folds are wholly contained in kx-gateway. Landing it separately keeps the context-rail PR reviewable and isolates the security-sensitive warrant/broker change with its own guard evidence. Additive; digest 7d22d4bd invariant (product digest ignores warrant_ref)."

[[feature]]
id       = "t-app-trigger-target"
lane     = "oss"
state    = "merged"
pr       = "290"
branch   = "feat/t-app-trigger-target"
covers   = [
  "T-APP-TRIGGER-TARGET (D178 pillar #4, the automation gap): a cron/webhook/gRPC trigger can fire a saved App (kortecx.app/v1), not just a recipe. Additive proto app_handle=8/timezone=9/require_approval=10 on RegisterTriggerRequest (+ 10/11/12 on TriggerView, back-compat; recipe_handle stays field 3). HostTriggerAdmin gains Option<Arc<dyn AppAuthor>>+Option<Arc<dyn RegisteredToolsView>>; submit() branches recipe binder.bind vs App author_app(party,app_handle,args,require_approval), ports the RunApp fireable-grant backstop, then the IDENTICAL register_run+submit_mote tail (coordinator stays sole writer). server.rs hoists the App-run resolver above the trigger seam (shared clone; None => App triggers fail closed + RunApp unimplemented). exactly-one-of recipe|app validated at register; App-target-without-seam fails fast",
  "5-field cron + IANA timezone (croner + chrono default-features-off + chrono-tz): schedule.rs next_fire() — legacy all-digits interval-seconds (ZERO migration) OR a 5-field crontab expr in a timezone (DST-correct); validated at register (bad expr/tz => synchronous invalid_argument, never a silent never-firing trigger). triggers.db SCHEMA_VERSION 1->2 (off-journal rebuild-empty) + app_handle/timezone/require_approval columns",
  "per-trigger HITL (D114, reuses the shipped machinery): require_approval threaded into AppAuthor::author_app; app_run.rs injects REACT_REQUIRE_APPROVAL_KEY (canonical-JSON bool) into the entry agentic step's config BEFORE lowering (part of the launch MoteId; false => nothing injected => byte-identical). Per-trigger cost_ceiling DEFERRED to PR-8 (blocked by T-WARRANT-COST-TLS-WIRE-DROP: WarrantSpec.cost_ceiling is intentionally not proto-encoded, convert.rs:579-581)",
  "first-class curated .with_slack()/.with_notion() App builders + provider catalog rows across CLI (verbs/connections.rs, + backfill the missing Discord CLI row)/Py(app.py)/TS(app.ts)/UI(ConnectionsPanel PROVIDERS); reference the kx-connector-slack/-notion sidecars (built in the parallel GR25 lane/integrations)",
  "cross-surface (D177 single-entry chaining): kx triggers add --app/--cron/--timezone/--require-approval (mutually exclusive --recipe|--app) + list render (app:/tz/hitl); Py kx.triggers.add(app_handle,timezone,require_approval); TS client.triggers.add({appHandle,timezone,requireApproval}); UI TriggersPanel recipe|app toggle + tz/HITL fields + app:/hitl row chips; docs/site/docs/authoring-a-trigger.md 'Firing an App on a schedule'",
  "gates: schedule.rs unit (interval/cron/tz/DST/bad) + triggers_store app/tz/hitl round-trip + app_run require_approval-injection-and-no-op + trigger_admin_e2e {app-target register+view · exactly-one-of · bad cron/tz · legacy interval} + CLI parse + Py/TS/UI cross-surface + LIVE dual-engine app_live_serve::trigger_fires_discord_app_live (a gRPC trigger fires a credentialed App to a terminal, both engines). Digest 7d22d4bd invariant (require_approval=false + recipe path byte-identical; triggers.db off-journal, NO journal/checkpoint change); frozen-trio EMPTY; additive-only proto + codegen-fresh; croner/chrono/chrono-tz cargo-deny clean",
]
inherits = ["app-pointer-run-resolution", "integrations-foundation", "autonomy-safety-gates", "gmail-integration", "discord-connector", "t-runapp-context-rail"]
notes    = "D178 completeness plan #4 (the automation gap), PULLED FORWARD ahead of the corpus's literal-next RC-SW3.1/RC-SW4 per the user's integrations-come-alive steer (2026-07-04). Realizes the persona-App-on-trigger vision (D170). MEDIUM blast radius: additive proto + a HostTriggerAdmin branch + a server.rs hoist + a new schedule.rs helper + the croner/chrono/chrono-tz dep subtree (server-binary; the cron ticker is host-owned; dep-wall + build-no-inference unaffected — iana-time-zone is pure-Rust on Linux CI). Off-journal + digest 7d22d4bd invariant by construction. Slack/Notion sidecars land in the path-disjoint lane/integrations lane (own PR; slack-connector/notion-connector rows). Deferred: per-trigger cost_ceiling => PR-8 (T-WARRANT-COST-TLS-WIRE-DROP); full approvals-inbox UI => RC6a; T-RUNAPP-RAG-RECIPE-ROUTE stays its own ticket."

[[feature]]
id       = "integrations-come-alive"
lane     = "oss"
state    = "merged"
pr       = "292"
branch   = "feat/integrations-come-alive"
covers   = [
  "connector binary discoverability (out-of-box dial): kx-mcp StdioTransport resolves a BARE connector program name (no path separator — e.g. 'kx-connector-slack' from a curated `--provider slack` connection) to a SIBLING of std::env::current_exe() first (+ EXE_SUFFIX for SN-7/Windows), then falls back to the OS PATH; a name that already contains a separator (absolute/relative — e.g. the witness harness's target/release path) is used verbatim. So a co-installed bundled connector dials without a manual PATH install (previously Command::new(bare) => TransportError::Unreachable). Resolution is spawn-time (round_trip + open_session Command::new sites); the stored `program` keeps author intent. GR8: additive (PATH fallback preserved), security-positive (a kx-sibling beats a bare-PATH lookup — narrows PATH-hijack surface), no env_clear/secret-injection change; kx-mcp is outside the projection closure => digest 7d22d4bd invariant, frozen-trio EMPTY, no journal/proto change",
  "RC6a approvals-inbox (D178 pillar #3 — govern autonomous trigger-firing Apps from the console): UI-only over the ALREADY-shipped+wired+E2E-tested ListPendingApprovals/GrantApproval/DenyApproval RPCs (autonomy-safety-gates #267). A Monitoring 'approvals' tab (beside 'alerts', the inbox precedent) — a use-approvals.ts hook (list poll + grant/deny mutations over client.approvals) + inbox list (tool_id/intent/deadline + Grant/Deny chip controls, all states) + a net-new count-badge slot on the Monitoring nav item (approvals are NOT on the event stream => poll). (Note: autonomy-safety-gates floated a Security placement; Monitoring chosen for the alerts-inbox + URL-addressable-tab precedent.)",
  "RC6a cost panel (M11 readout): a use-run-cost.ts hook + a display-only run-cost readout (turns/tool_calls/estimated·ceiling micro-USD/over_ceiling) over the already-shipped GetRunCost RPC (client.cost). Honest empty state at zero-baseline pricing",
  "GR28 dual-engine live witnesses (the come-alive proof, PR1 sidecars land the binaries): app_live_serve.rs thin ConnectorCase callers runapp_slack/notion_* + trigger_fires_slack/notion_app_live — a live Gemma App fires slack/read_channel + notion/search on BOTH engines in offline FAKE mode (no creds). Observe-witness posture (settle-to-terminal + SN-8 granted-tool + honest per-engine log)",
  "cross-surface (D177): the approvals/cost SDK surface already ships (kx approvals/cost CLI · kx.approvals/kx.cost Py · client.approvals/client.cost TS); PR2 adds the UI leg + docs/site chaining page (connect --provider slack -> .with_slack().secrets([...]) -> trigger add --app --require-approval -> approve from the console)",
  "gates: kx-mcp resolve_program unit tests (separator-verbatim/sibling-hit/PATH-fallback) + vitest (use-approvals/use-run-cost hooks, inbox render + grant/deny, cost readout, nav badge) + Playwright approvals-tab grant e2e + nav-model invariant + a0_guard 7d22d4bd + frozen-trio EMPTY + pool_determinism + kx-profile resolution timing + GR28 dual-engine live (both engines, models restarted) + out-of-box sibling-dial e2e",
]
inherits = ["autonomy-safety-gates", "t-app-trigger-target", "slack-connector", "notion-connector", "integrations-foundation", "connector-extension-sdk"]
notes    = "D178 'integrations come alive + watchable' (user-directed 2026-07-04, this session = come-alive-now / RC-SW4-next). Bundles three cohesive, well-integrated pieces that turn shipped machinery into a usable+governable+proven experience: (2A) the out-of-box connector dial fix [the real 'come alive' gap — a real user's --provider slack failed Unreachable without a manual PATH install], (2B) the RC6a approvals-inbox + cost panel [makes trigger-firing autonomous Apps governable/watchable — UI over shipped RPCs, ~zero backend blast radius], (2C) the GR28 dual-engine live witnesses [proof an App fires slack/notion on both engines]. Shared lane (feat/*); depends on the path-disjoint lane/integrations sidecar PR #291 (connector crates) for 2C to RUN. Additive; digest 7d22d4bd invariant (2A off-projection-closure kx-mcp; 2B UI-only; 2C test-only); frozen-trio EMPTY. Deferred: swarm trace-tree (RC6a-continuation, no precedent) · T-RUNAPP-RAG-RECIPE-ROUTE (own small PR) · respond_json/registry-install (later RC6a)."

[[feature]]
id       = "gemma3-connector-tool-fire"
lane     = "oss"
state    = "merged"
pr       = "293"
branch   = "feat/gemma3-connector-tool-fire"
covers   = [
  "The gemma3 connector-tool-fire fix (T-RUNAPP-RAG-RECIPE-ROUTE generalized from RAG retrieve@1 to ARBITRARY connector/skill tool-calls, L-006/L-016): make Ollama gemma3:12b RELIABLY FIRE tools on the live agentic loop (was: slack answers-without-firing; notion dead-letters 'no admissible turn') while llama.cpp Gemma-4 already fired",
  "Lever A — shared tool-use priming: a TOOL_STEER_DIRECTIVE const appended to the OFF-DIGEST tool menu (kx-gateway model_exec.rs react_tool_menu) — reaches BOTH the connector agentic-launch turn AND react-rag on BOTH engines from one choke point; ephemeral/off-MoteId; respects the provision.rs NOTE (name the tools, never inline the {tool_call} envelope). Fixes the answer-without-firing symptom",
  "Lever B — Ollama non-strict UNION format (the firing guarantee; the Ollama analog of llama.cpp's lazy GBNF): a new {\"tool_call\":{…}} oneOf {\"answer\":\"…\"} whole-response `format` forces gemma3 to emit PARSEABLE JSON (a well-formed tool_call fires OR a well-formed answer settles) — never a malformed dead-letter, without forcing tool-required (unlike KX_SERVE_OLLAMA_TOOL_FORMAT strict mode). kx-grammar ToolEnvelopeSpec.answerable flag (skip_serializing_if=is_false ⇒ default carrier byte-identical) + to_ollama_union_format/render_union; kx-ollama response_format answerable arm; build_tool_grammar arms answerable=!strict && KX_SERVE_OLLAMA_TOOL_UNION (default-ON kill-switch); kx-toolcall extract_answer helper (pure, additive — {\"answer\"} was already Ok(None), NO classification change); coordinator finalize_agentic_launch + CLI wait.rs display/commit unwrap of the answer arm",
  "bundle: feature-ledger reconcile (slack/notion #291, integrations-come-alive #292 in-progress→merged) + .with_slack()/.with_notion()/.with_discord() connection-NAME tool-grant docstrings + SDK test grants (slack/read_channel not kx-connector-slack/*) + `kx connections doctor` local advisory (reuses kx-mcp resolve_program_with) + UI chip namespace hint + richer GR28 dual-engine witnesses (multi-tool read→post/search→create + trigger+approval + mixed MCP+connector; gemma3 now HARD-asserts tool_fired)",
  "gates: render_union/to_ollama_union_format/extract_answer/response_format-union-arm units + carrier round-trip (answerable) + deterministic connector-fire wiring gate + a0_guard 7d22d4bd INVARIANT + frozen-trio EMPTY + no journal/proto change + kx-profile Ollama-vs-llama.cpp decode + GR28 dual-engine live (both engines restarted; gemma3 FIRES slack/notion/retrieve)",
]
inherits = ["integrations-come-alive", "slack-connector", "notion-connector", "t-runapp-context-rail"]
notes    = "User-locked 2026-07-04 (mechanism decision: 'Priming + Ollama union format'). Digest 7d22d4bd invariant by construction — the grammar carrier rides the dispatch-time params clone (model_exec.rs:1391→1408), NEVER the durable MoteDef (build_agentic_turn leaves grammar:None); the priming rides the ephemeral off-MoteId tool menu. Frozen-trio (kx-scheduler/kx-executor/kx-inference) EMPTY: gbnf::render reads only spec.tools, so `answerable` never enters the GBNF/lazy-arm. Cost accepted: the Ollama tool turn loses free-form CoT (fine for tool-decision turns; kill-switch KX_SERVE_OLLAMA_TOOL_UNION; Ollama-only; tools-granted-only). SN-5 docs/plans/2026-07-04-gemma3-connector-tool-fire.md."

[[feature]]
id       = "rc-sw4-supervisor-consensus"
lane     = "oss"
state    = "planned"
covers   = [
  "RC-SW4 min-viable orchestration: supervisor + consensus + core patterns; DEFER handoff()/advanced debate/graph post-RC (the one digest-sensitive item) — user-locked 2026-07-05",
  "cross-surface bundle same-PR (D143): swarm/team on CLI+UI · respond_json/--json-schema · chat-attach-tools",
]
inherits = ["oss-cloud-sync-infra"]
notes    = "The next OSS RC code PR per HANDOFF §2.312/§2.315 + D178. Registered here (readiness-audit C3) so a concurrent OSS session does not double-grab it. B-spec docs/plans/2026-07-05-rc-sw4-supervisor-consensus-plus-cross-surface-bspec.md. Serialized shared lane — start only after the registry-reconcile PR merges."

[[feature]]
id       = "t-gemma3-tool-loop-answer-force"
lane     = "oss"
state    = "merged"
branch   = "feat/gemma3-tool-loop-answer-force"
pr       = "294"
covers   = [
  "T-GEMMA3-TOOL-LOOP-ANSWER-FORCE (loop-completeness follow-up to gemma3-connector-tool-fire #293, L-019): once gemma3:12b FIRES (via the #293 union), it loops — re-firing the SAME tool with identical args on the observation turn; kx_toolcall dedup refuses it, but gemma3 re-proposes the identical call and dead-letters under budget (llama.cpp Gemma-4 already completes fire→answer). Fix: on the next react turn AFTER a duplicate-call rejection OR the near-budget settle-nudge, arm an ANSWER-ONLY Ollama union ({\"answer\":\"…\"}-only, tool_call arm DROPPED) so a weak model is FORCED to settle instead of looping",
  "GATEWAY-DERIVED, off-digest (two adversarial plan reviews converged on this over coordinator-carried plumbing): the reprompt/nudge decision is ALREADY serialized by the coordinator into the turn's on-digest instruction (config_subset[PROMPT_KEY] via render_reprompt/render_settle_nudge); model_exec.rs react_answer_force(mote) reads prompt_from_config(mote) and matches the shared kx_toolcall markers — DRIFT-PROOF (same bytes the build wrote; no re-computed loop-counter twin) + NO proto/worker/ContextSink/coordinator plumbing. FIRST-duplicate arming (user-chosen): GR15-safe since the tool result already exists in context, so the forced answer draws on real data",
  "kx-grammar ToolEnvelopeSpec.answer_only flag (skip_serializing_if=is_false ⇒ default carrier byte-identical) + render_answer_only (DRY-shares answer_arm() with render_union) + to_ollama_answer_only_format; kx-ollama response_format e.answer_only arm (BEFORE the answerable arm — stronger constraint wins); kx-toolcall DUPLICATE_REJECT_MARKER + SETTLE_NUDGE_MARKER shared consts + is_duplicate_reason; kx-gateway build_tool_grammar(warrant, answer_force) with answer_only=union&&answer_force&&KX_SERVE_OLLAMA_ANSWER_FORCE and answerable=union&&!answer_only (STRUCTURAL mutual exclusivity) + a separate default-ON kill-switch (independent rollback of the answer-force without losing the #293 fire fix)",
  "gates: kx-grammar render_answer_only/carrier-round-trip/gbnf_ignores_answer_only (llama.cpp no-op) + kx-ollama answer_only-arm/≤1-of-3-modes + kx-toolcall is_duplicate_reason/uniqueness/truncation-guard + model_exec react_answer_force fires-on-dup/nudge-not-base/bad-args + build_tool_grammar arms-answer_only + kx-coordinator render_output_carries_the_shared_answer_force_markers (coupling) + react_live duplicate_reprompt_instruction_carries_the_answer_force_marker (deterministic full-seam) + a0_guard 7d22d4bd INVARIANT + frozen-trio EMPTY + no journal/proto change. GR28 dual-engine LIVE: (Ollama) kx-ollama answer_force_live PROVES the answer-only format FORCES gemma3 to settle ({\"answer\"}) vs the union firing on the identical prompt; (llama.cpp) gbnf no-op + fires+completes ~80% (Metal FP nondeterminism, byte-identical carrier ⇒ NOT regressed)",
]
inherits = ["gemma3-connector-tool-fire", "integrations-come-alive", "slack-connector", "notion-connector"]
notes    = "Loop-completeness follow-up ticketed in #293 (L-019). Digest 7d22d4bd invariant by construction — answer_force is derived at dispatch from the on-digest instruction and only shapes params.grammar (the dispatch clone), NEVER the MoteDef. Frozen-trio EMPTY: gbnf::render reads only spec.tools so `answer_only` never enters the GBNF ⇒ llama.cpp byte-unchanged (it already completes the loop; this is the gemma3-specific gap). Ollama-only; tools-granted-only; kill-switch KX_SERVE_OLLAMA_ANSWER_FORCE (default-ON). ★ LIVE TESTING FINDING (GR28): gemma3's DOMINANT connector-loop failure is MISSING REQUIRED TOOL ARGS (emits args:{} → the observation fails → an HONEST dead-letter at turn 0, BEFORE any reprompt/nudge — correct per GR15), a SEPARATE deferred gap ticketed T-GEMMA3-OLLAMA-TOOL-ARG-SCHEMA (constrain the Ollama union tool_call args to each tool's inputSchema required fields). So the app_live_serve `require_answer` witness is SOFT-OBSERVE (log, not gate) — a hard 'always answers' would be invalid (GR15). The answer-force itself is proven by the deterministic full-seam + the answer_force_live contrast. SN-5 docs/plans/2026-07-05-gemma3-tool-loop-answer-force.md (private)."
